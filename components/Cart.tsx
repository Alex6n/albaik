import { ShoppingCart } from "lucide-react";
import React from "react";

const Cart = () => {
  return (
    <ShoppingCart className="text-white duration-700 transition-all hover:text-primary hover:border-primary hover:cursor-pointer rounded-xl p-1 w-9 h-9 hover:bg-black/20" />
  );
};

export default Cart;
